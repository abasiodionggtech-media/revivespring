PLACE THE ONBOARDING INTRO VIDEO HERE.

Exact steps:
1. Take your file "INTRODUCTION VIDEO 3.MP4"
2. Rename it to:  intro-video.mp4
   (all lowercase, hyphen instead of spaces, lowercase .mp4 extension —
   this matters because the server this runs on is case-sensitive and
   doesn't like spaces in URLs)
3. Put the renamed file directly in this folder, so the path is:
   backend-reset-work/revivespring-main/revivespring-main/public/media/intro-video.mp4
4. Commit and push it to GitHub along with the rest of your code, same as
   any other change.
5. Once deployed on Render, the video will be reachable at:
   https://revivespring.onrender.com/media/intro-video.mp4

You can delete this file (PLACE_VIDEO_HERE.txt) once the real video is in
this folder — it's just a placeholder so the empty folder doesn't get
lost when committed to git (git doesn't track empty folders).
